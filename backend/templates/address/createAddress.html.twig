{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Create Address</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Save Changes</button>
        {{ form_end(form) }}
        <a href="{{ path('user_addresses') }}" class="btn btn-secondary">Cancel</a>
    </div>
{% endblock %}
